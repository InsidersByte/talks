{
  "name": "an-introduction-to-react",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "normalize.css": "5.0.0",
    "react": "15.4.2",
    "react-dom": "15.4.2",
    "spectacle": "2.0.0"
  },
  "devDependencies": {
    "react-scripts": "0.9.0",
    "surge": "0.18.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "surge ./build succinct-string.surge.sh"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
